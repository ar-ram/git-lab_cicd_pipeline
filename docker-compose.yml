version: "3.8"

services:
  node-app:
    container_name: node_app
    image: ram0810/node-app:latest  # 🔹 Use the pulled image instead of building
    ports:
      - "3000:3000"
    restart: always
